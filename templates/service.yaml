apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-test-nd.fullname" . }}
  labels:
    {{- include "argo-test-nd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-test-nd.selectorLabels" . | nindent 4 }}
