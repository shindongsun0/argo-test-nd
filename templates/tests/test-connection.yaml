apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-test-nd.fullname" . }}-test-connection"
  labels:
    {{- include "argo-test-nd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-test-nd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
